# Public mTBI-OS Supabase project. The browser only ever holds the publishable
# key; every table has RLS and the site reads only the aggregate funding RPC.
# These have safe defaults baked into src/lib/supabase.ts — set them here only
# to override (e.g. a Supabase preview branch).
VITE_SUPABASE_URL=https://hwemawdopovlvuhjkjke.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_J9fcAlPCt_AeAtelrwGhmQ_O5BxHhnJ
